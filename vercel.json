{
  "version": 2,
  "routes": [
    { "src": "/", "dest": "/api/listings.js" },           // root maps to listings
    { "src": "/listings/(.*)", "dest": "/api/listings/$1" }  // dynamic detail pages
  ]
}
